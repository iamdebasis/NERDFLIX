node_modules/
dist/
out/
# Packaged builds — a 122 MB DMG does not belong in git.
release/
*.log
.DS_Store

# Secrets. .env.example is committed as the template.
.env

# Library data — yours, not the project's. See data/README.md.
/data/db/
/data/state/
/data/cache/
