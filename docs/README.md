# docs/

`screenshots/` — captured from the running app, referenced by the root README.

They are regenerated by driving the real UI over Chrome DevTools Protocol rather than
posed by hand, so what the README shows is what the app actually renders. Each capture
asserts the DOM is in the state it claims to be showing before it writes a file — an
empty modal or a billboard that never started is worse than no screenshot at all.
